<% if obj.image.attached? %>
  <%= image_tag obj.image.variant(resize: "400", gravity: "center", crop: "400x225+0+0").processed, class: "own-rooms-list-image" %>
<% else %>
  <%= image_tag "default_room_image.png", class: "own-rooms-list-image" %>
<% end %>
